{
  "name": "@ascendry/sdk",
  "version": "1.0.0",
  "description": "SDK for the Ascendry API (https://ascendry.io/)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prepare": "npm run clean && npm run build",
    "format": "prettier --write 'src/**/*.{js,ts,json,md}'"
  },
  "keywords": [
    "typescript",
    "models",
    "ascendry",
    "api",
    "sdk"
  ],
  "author": "Rastaar Haghi",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "devDependencies": {
    "typescript": "^5.0.0",
    "prettier": "^3.3.2"
  },
  "dependencies": {
    "axios": "^1.9.0"
  }
}
